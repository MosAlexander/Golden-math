Run the full GoldenMatch demo pipeline and analyze results:

1. Run `python -m src.demo_pipeline`
2. Parse the VALIDATION section
3. Report accuracy, missed matches, and false positives
4. If accuracy < 80%, suggest specific fixes
5. Compare with previous run if results exist in docs/pipeline_runs.log
