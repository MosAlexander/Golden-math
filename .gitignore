# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Tests / coverage
.pytest_cache/
.coverage
htmlcov/

# Venv
.venv/
venv/
env/
.env

# Streamlit secrets (токены — не коммитить)
.streamlit/secrets.toml

# Сертификат Минцифры — локальный, не коммитить
*.cer
*.crt

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Project artifacts (создаются на лету)
data/last_run.json
data/channels.json
data/llm_config.json
data/llm_verdicts.json
data/runs/
docs/pipeline_runs.log

# Temporary files (мусор после ручных проверок)
before_*.txt
after_*.txt
tmp_*.py
